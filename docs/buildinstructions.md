From Src Folder:

`cmake -S . -B ../build`

then

`cmake --build ../build`

then

`cd ../build`

Now, a few options:

Run individual test:
`./hello_test`
Run program:
`./MusiDoodle`
Run all tests:
`ctest`